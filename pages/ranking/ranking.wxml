<!--pages/ranking/ranking.wxml-->
<view class="box">
  <view class="modek" bindtap="goHorizontal"><image src="/images/mode.png"></image></view>
  <view class="table">
    <view class="tr bg-w">
      <view class="th orange">TOP</view>
      <view class="th orange">商品名称</view>
      <view class="th orange">销售额(元)</view>
      <view class="th orange">订单数</view>
    </view>
    <block wx:for="{{listData}}" wx:key="{[code]}">
      <view class="tr bg-g" wx:if="{{index % 2 == 0}}">
        <view class="td">{{item.code}}</view>
        <view class="td">{{item.text}}</view>
        <view class="td">{{item.price}}</view>
        <view class="td">{{item.num}}</view>
      </view>
      <view class="tr" wx:else>
        <view class="td">{{item.code}}</view>
        <view class="td">{{item.text}}</view>
        <view class="td">{{item.price}}</view>
        <view class="td">{{item.num}}</view>
      </view>
    </block>
  </view>
</view>